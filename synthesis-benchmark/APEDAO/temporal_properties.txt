box(transferToken(t, from, to, n) -> tokenBalance(to, b) -> b > •b) box(transferLP(from, to, n) -> lpBalance(to, b) -> b > •b) box(transferToken(t, from, to, n) -> tokenBalance(from, b) -> b < •b) box(transferLP(from, to, n) -> lpBalance(from, b) -> b < •b) box(stakeNFT(p, id) -> nftBalance(p, b) -> b < •b) box(unstakeNFT(p, id) -> nftBalance(p, b) -> b > •b) box(nftRewardDistribution(p, n) -> tokenBalance(p, b) -> b > •b) box(transferToken(t, from, to, n) -> taxLp(tax) && taxNFT(taxN) && tokenBalance(from, b) -> b == •b - (n + tax + taxN)) box(isDividendExempt(p) -> transferToken(_, p, _, _) -> false) box(isIdoUser(p) -> removeLiquidity(p, _) -> false)