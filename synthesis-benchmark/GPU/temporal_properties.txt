box(transferFrom(from, to, spender, value) -> allowance(from, spender, a) && a >= value) box(transfer(from, to, value) -> balanceOf(from, b) && b >= value) box(burn(p, amount) -> balanceOf(p, b) && b >= amount) box(swapAndLiquify(tokenAmount) -> balanceOf(contract, c) && c >= tokenAmount) box(transfer(from, to, value) -> (_isPairs(from) -> balanceOf(_destroyAddress, d) && d' == d + value * 2 / 100) && (_isPairs(to) -> balanceOf(_destroyAddress, d) && d' == d + value / 200))