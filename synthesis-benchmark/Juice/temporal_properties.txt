box(stake(p, amount, stakeWeek) -> stakingStartTime(t) && t > 0) box(stake(p, amount, stakeWeek) -> stakingEndTime(t) && now() <= t) box(harvest(p, stakeIndex) -> unstakeStatus(p, stakeIndex, 0)) box(unstake(p, stakeIndex) -> endTime(p, stakeIndex, e) && now() >= e) box(harvest(p, stakeIndex) -> stakedAmount(p, stakeIndex, a) && a > 0) box(harvest(p, stakeIndex) -> balanceOf(contract, c) && c >= pendingReward(p, stakeIndex, _, _)) box(stake(p, amount, _) -> balanceOf(p, b) && b >= amount) box(unstake(p, stakeIndex) -> unstakeStatus(p, stakeIndex, 1)) box(stake(p, amount, _) -> balanceOf(contract, c) && c' == c + amount) box(unstake(p, stakeIndex) -> balanceOf(contract, c) && c' == c - stakedAmount(p, stakeIndex, _))