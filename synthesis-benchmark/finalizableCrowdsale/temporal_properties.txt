//Ensuring total balances do not exceed total supply box(totalSupply(n) && totalBalances(s) -> s <= n)
//Ensuring allowance cannot be exceeded box(allowance(o, s, n) && transferFrom(o, r, s, m) -> m <= n)
//Ensuring no negative balances box(balanceOf(p, n) -> n >= 0)
//Ensuring total balances match total supply box(totalBalances(s) && totalSupply(n) -> s == n)
//Ensuring finalization only occurs once box(onceFinalize(true) -> !diamond(finalize()))
//Ensuring no token purchase after finalization box(onceFinalize(true) -> !diamond(buyToken(_, _)))
//Ensuring illegal finalization is prevented box(onceFinalize(true) -> now(t) && end(e) && t >= e)