box(depositNative(p, n) -> nativeBalance(p, b) -> b > •b) box(depositToken(p, t, n) -> tokenBalance(t, p, b) -> b > •b) box(withdrawNative(p, n) -> nativeBalance(p, b) -> b < •b) box(withdrawToken(p, t, n) -> tokenBalance(t, p, b) -> b < •b) box(lockAccount(p, _, _) -> withdrawNative(p, _) -> false) box(lockAccount(p, _, _) -> withdrawToken(p, _, _) -> false) box(transferNative(from, to, n) -> nativeBalance(from, b1) && nativeBalance(to, b2) -> b1 < •b1 && b2 > •b2) box(transferToken(t, from, to, n) -> tokenBalance(t, from, b1) && tokenBalance(t, to, b2) -> b1 < •b1 && b2 > •b2) box(lockAccount(p, _, _) -> transferNative(p, _, _) -> false) box(lockAccount(p, _, _) -> transferToken(_, p, _, _) -> false) box(releaseNative(p, n) -> pendingNativeBalance(p, b1) && nativeBalance(p, b2) -> b1 < •b1 && b2 > •b2) box(releaseToken(t, p, n) -> pendingTokenBalance(t, p, b1) && tokenBalance(t, p, b2) -> b1 < •b1 && b2 > •b2)