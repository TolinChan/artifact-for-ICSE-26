box(stakeToken(user, planId, amount, _) -> tokenBalance(stakingToken, user, b) -> b < •b) box(stakeToken(user, planId, amount, _) -> tokenBalance(stakingToken, address(this), b) -> b > •b) box(unstakeToken(user, planId, amount, _, _) -> tokenBalance(stakingToken, user, b) -> b > •b) box(claimRewards(user, planId, _, _) -> tokenBalance(stakingToken, user, b) -> b > •b) box(claimRewards(user, planId, _, _) -> tokenBalance(stakingToken, address(this), b) -> b < •b) box(unstakeToken(user, planId, amount, burnRate, _) -> burnAmount == amount * burnRate / 100 -> tokenBalance(stakingToken, burnAddress, b) -> b > •b) box(emergencyWithdraw(user, planId, amount, _) -> burnAmount == amount * penalty / 100 -> tokenBalance(stakingToken, burnAddress, b) -> b > •b)