box(claimRewards(p, n, true) -> totalStaked(m) && maxPercentage(mp) && n <= (m * mp / 100)) box(unstake(p, i, _) -> stake(p, _, t) && now(d) && d >= t) box(stake(p, amount, lockPeriod) -> amount > 0) box(claimRewards(p, n, _) -> balanceOf(p, b) && n <= b) box(claimAffiliateEarnings(p, amount) -> amount >= 0) box(claimRewards(p, n, _) -> totalRewardsClaimed(s) && s' == s + n) box(claimRewards(p, n, _) -> maxPercentage(mp) && balanceOf(p, b) && n <= (b * mp / 100)) box(claimRewards(p, n, _) -> balanceOf(p, b) && b' == b - n && b' >= 0) box(unstake(p, i, amount) -> totalStaked(s) && s' == s - amount && s' >= 0) box(unstake(p, i, amount) -> stake(p, m, _) && m >= amount)